import { Metadata } from "next";

export const metadata: Metadata = {
   title: "loading",
   description: "Just an animated loading spinner.",
};

export { default as default } from "../database/[databaseId]/table/[tableName]/loading";
